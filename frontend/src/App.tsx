/**
 * App — root of the GPSR Compliance Engine admin (embedded in Shopify Admin).
 *
 * Scaffold only (feature F0): renders a placeholder shell. Real screens
 * (entity/template library, classification rules, bulk product editor) arrive
 * in F4–F6.
 */
export function App() {
  return (
    <main>
      <h1>GPSR Compliance Engine</h1>
      <p>Admin scaffold ready.</p>
    </main>
  );
}
