# Copy to .env (never commit .env). Documents required keys only — no real secrets.

# --- MySQL (port 3308) ---
DB_HOST=127.0.0.1
DB_PORT=3308
DB_NAME=gpsr
DB_USER=gpsr
DB_PASSWORD=change_me

# --- Backend ---
BACKEND_PORT=8000
GIN_MODE=debug

# --- Shopify app ---
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_APP_URL=
SHOPIFY_SCOPES=read_products,write_products
