// Package repository is the data-access layer (MySQL on port 3308).
// It contains SQL/queries only and returns models or errors — no business rules.
// Handlers must never import it directly; access goes through the service layer.
//
// Placeholder for F1–F3; no implementations yet.
package repository
